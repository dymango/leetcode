package leetcodepractice.leetcode;

/**
 * @author dimmy
 */
public class Multiply_43 {

    /**
     * 给定两个以字符串形式表示的非负整数 num1 和 num2，返回 num1 和 num2 的乘积，它们的乘积也表示为字符串形式。
     * 注意：不能使用任何内置的 BigInteger 库或直接将输入转换为整数。
     * <p>
     *  
     * <p>
     * 示例 1:
     * <p>
     * 输入: num1 = "2", num2 = "3"
     * 输出: "6"
     * 示例 2:
     * <p>
     * 输入: num1 = "123", num2 = "456"
     * 输出: "56088"
     *  
     * <p>
     * 提示：
     * <p>
     * 1 <= num1.length, num2.length <= 200
     * num1 和 num2 只能由数字组成。
     * num1 和 num2 都不包含任何前导零，除了数字0本身。
     * <p>
     * 来源：力扣（LeetCode）
     * 链接：https://leetcode.cn/problems/multiply-strings
     * 著作权归领扣网络所有。商业转载请联系官方授权，非商业转载请注明出处。
     *
     * @param num1
     * @param num2
     * @return
     */
    public String multiply(String num1, String num2) {
//        char[] nums1Arr = num1.toCharArray();
//        char[] nums2Arr = num2.toCharArray();
//        int length = Math.min(nums2Arr.length, nums1Arr.length);
//        int maxL = Math.max(nums2Arr.length, nums1Arr.length);
//        char[] result = new char[maxL];
//        int temp = 0;
//        for (int i = nums1Arr.length - 1; i >= 0; i--) {
//            int nums1 = nums1Arr[i] -
//        }
        return null;
    }
}
